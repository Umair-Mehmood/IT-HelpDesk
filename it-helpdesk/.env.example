# Hostinger MySQL (set these in Vercel → Project → Settings → Environment Variables)
DB_HOST=srv1393.hstgr.io
DB_NAME=IT_Helpdesk
DB_USER=itmanagement
DB_PASSWORD=your_password_here
DB_PORT=3306

# Frontend uses /api on the same Vercel domain — no VITE_API_BASE needed in production
