# Hostinger MySQL — use the full prefixed names from hPanel
DB_HOST=srv1393.hstgr.io
DB_NAME=u691790690_IT_Helpdesk
DB_USER=u691790690_itmanagement
DB_PASSWORD=your_password_here
DB_PORT=3306

# Set the same values in Vercel → Project → Settings → Environment Variables
# Frontend uses /api on the same Vercel domain — no VITE_API_BASE needed in production
